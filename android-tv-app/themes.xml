<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base TV Theme -->
    <style name="Theme.PlayraTV" parent="Theme.Leanback">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:windowBackground">@color/default_background</item>
        <item name="android:colorBackground">@color/default_background</item>
    </style>

    <!-- Main Activity Theme -->
    <style name="Theme.PlayraTV.Main" parent="Theme.PlayraTV">
        <item name="browseTitleViewLayout">@layout/title_view</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Player Activity Theme -->
    <style name="Theme.PlayraTV.Player" parent="Theme.PlayraTV">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- No Title Theme -->
    <style name="Theme.PlayraTV.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
